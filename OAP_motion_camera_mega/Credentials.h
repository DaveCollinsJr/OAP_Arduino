#ifndef __CREDENTIALS_H__
#define __CREDENTIALS_H__

// Ethernet parameters
char serverName[] = "www.oneassetplace.com";
//char serverName[] = "192.168.0.103";
int serverPort = 80;   // Usually 80 unless directed otherwise

// This is the mac address of YOUR ethernet shield with each 2-digit hex number between dashes as its own hex element
byte mac[] = { 0x90, 0xA2, 0xDA, 0x0D, 0x9B, 0x33 };  
#endif
